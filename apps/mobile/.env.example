# Copy to .env (gitignored). Leave EXPO_PUBLIC_SUPABASE_URL empty to run in demo mode (Expo Go, no backend).
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
# Optional: OSRM endpoint for walk / cycle / drive directions (empty = deterministic mock, works offline).
EXPO_PUBLIC_OSRM_URL=
